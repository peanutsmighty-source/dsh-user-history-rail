# ARR v0.2 Pilot

此清单冻结 Pilot 的任务 ID、提示、难度、验证命令与策略。每次真实运行必须先把对应 fixture 复制到新的临时目录；不得在 ARR 或 DSH 主工作区执行任务。

Pilot 目标是检查执行链、JSONL 和判定器，不用于宣称成功率非劣或成本优势。真实 fixture 与执行结果会在清单审核后加入，审核后不得修改已执行任务的 prompt、validator 或难度标签。
